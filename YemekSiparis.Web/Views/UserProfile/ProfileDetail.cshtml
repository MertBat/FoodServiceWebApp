@using YemekSiparis.Core.Entities;
@using YemekSiparis.Core.Enums;
@{
    ViewData["Title"] = "Profil Güncelleme";
}

@model Customer

<h1>CLASSROMS</h1>
<hr />
<br />

<div class="row">
    @*     @if (TempData["Basarılı"] != null)
    {
    <div class="row justify-content-md-center ">
    <div class="col-sm-2 d-flex justify-content-center" style="background:green; margin-bottom:10px; border-radius:5px; align-items:center">

    @TempData["Basarılı"].ToString()
    @if (@TempData["Uyarı"] != null)
    @TempData["Uyarı"].ToString()

    </div>
    </div>
    } *@
    <div class="col-lg-11 offset-1 ">
        <table class=" table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Yaşı</th>
                    <th>Doğum Tarihi</th>
                    <th>Cinsiyeti</th>
                    <th>Adresi</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>@Model.Name</td>
                    <td>@Model.Age</td>
                    <td>@Model.Birthdate</td>
                    <td>@Model.Gender.ToString()</td>
                    <td>@Model.Address</td>
                </tr>
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="9">
                        <a class="btn btn-success" asp-action="ProfileUpdate" asp-route-id="@Model.Id" style="width:100%">Güncelle</a>
                    </td>
                </tr>
            </tfoot>
        </table>
    </div>
</div>



<div id="ListStudent">
</div>

@* <td>
    <select asp-for="@Model.Gender" class="form-control">
        @foreach (var item in Enum.GetValues(typeof(Gender)))
        {
            <option value="@item">@item</option>
        }
    </select>
</td> *@